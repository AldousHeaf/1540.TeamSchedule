{
  "name": "competition-schedule-organizer",
  "version": "1.0.0",
  "description": "FRC Team 1540 Competition Schedule Organizer",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "node build-static.js",
    "deploy": "node build-static.js && git add docs/ schedule.csv && git commit -m \"Update schedule from responses\" || true"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
